node_modules/
dist/
work/
*.log
.env
.env.local
.DS_Store


# Persisted store (contains secrets) — never commit
data/

# TypeScript incremental build cache
*.tsbuildinfo

# The studio SPA lives in 911RS/tabligh-studio; `npm run studio:fetch` clones it here.
web/
